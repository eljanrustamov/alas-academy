import styled from "styled-components";

export const CtaAreaStyled = styled.div`
  padding: 102px 0;
  background-color: #f0f4f5;
  position: relative;
  z-index: 1;
  text-align: center;
  overflow-x:hidden;

  .section-title {
    .title{
        font-weight: 700;
        font-size: 36px;
        margin-bottom: 28px;
        line-height: 1.39;

        span{
            color: var(--color-primary);
        }
    }
  }
`;
